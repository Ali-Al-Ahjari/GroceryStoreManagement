<UserControl x:Class="GroceryStoreManagement.Windows.ReportsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf" FlowDirection="RightToLeft" Language="ar-YE" Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- العنوان -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="CurrentReportIcon" Text="&#xE9D9;" FontFamily="{StaticResource IconFont}" FontSize="26" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock x:Name="CurrentReportTitle" Text="التقارير والإحصائيات" Style="{StaticResource HeaderLabel}"/>
                    <TextBlock x:Name="CurrentReportSubtitle" Text="عرض وتصدير التقارير المختلفة" Style="{StaticResource BodyLabel}" Margin="0,5,0,0"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <!-- الفلاتر العامة -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,20" Padding="15">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <Grid HorizontalAlignment="Left" MinWidth="1150">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="من تاريخ:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker x:Name="FromDatePicker" Width="130" FlowDirection="LeftToRight"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="إلى تاريخ:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker x:Name="ToDatePicker" Width="130" FlowDirection="LeftToRight"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="نوع التقرير:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="CmbReportType" Width="180" Style="{StaticResource ModernComboBox}" SelectionChanged="CmbReportType_SelectionChanged">
                        <ComboBoxItem Content="تقرير المبيعات" IsSelected="True"/>
                        <ComboBoxItem Content="تقرير المشتريات"/>
                        <ComboBoxItem Content="تقرير المخزون"/>
                        <ComboBoxItem Content="تقرير العملاء"/>
                        <ComboBoxItem Content="تقرير الأرباح"/>
                        <ComboBoxItem Content="سجل النشاطات"/>
                        <ComboBoxItem Content="تقرير المنتجات الأكثر مبيعاً"/>
                        <ComboBoxItem Content="الفواتير غير المدفوعة"/>
                    </ComboBox>
                </StackPanel>

                <!-- فلاتر إضافية ديناميكية -->
                <StackPanel x:Name="AdditionalFilterPanel" Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button x:Name="BtnGenerateReport" Style="{StaticResource PrimaryButton}" Padding="15,10" Margin="0,0,10,0" Click="BtnGenerateReport_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE9D2;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="إنشاء التقرير" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnExportExcel" Style="{StaticResource OutlineButton}" Padding="15,10" Margin="0,0,10,0" Click="BtnExportExcel_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE896;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="تصدير" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnPrint" Style="{StaticResource OutlineButton}" Padding="15,10" Click="BtnPrint_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE749;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="طباعة" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>

        <!-- محتوى التقارير -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- الرسم البياني والجدول -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- الرسم البياني -->
                <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,15" Height="300">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <!-- رسم بياني خطي/عمودي -->
                        <Grid Grid.Column="0" Margin="0,0,10,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="ChartTitle" Text="تحليل البيانات" Style="{StaticResource SubHeaderLabel}" Margin="0,0,0,10"/>
                            <lvc:CartesianChart x:Name="MainChart" Grid.Row="1" LegendLocation="None"/>
                        </Grid>

                        <!-- رسم بياني دائري -->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="PieChartTitle" Text="التوزيع" Style="{StaticResource SubHeaderLabel}" Margin="0,0,0,10"/>
                            <lvc:PieChart x:Name="PieChart" Grid.Row="1" LegendLocation="Bottom" Hoverable="True" InnerRadius="0"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- الجدول -->
                <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Padding="15,10" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <Grid>
                                <TextBlock x:Name="DataGridTitle" Text="التفاصيل" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="DataGridCount" Text="0 سجل" HorizontalAlignment="Left" Foreground="{DynamicResource TextMutedBrush}"/>
                            </Grid>
                        </Border>
                        <DataGrid x:Name="ReportDataGrid" Grid.Row="1" Style="{StaticResource ModernDataGridStyle}" BorderThickness="0"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- ملخص التقرير (Generic Cards) -->
            <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="&#xE8C8; ملخص التقرير" FontFamily="{StaticResource IconFont}" Style="{StaticResource SubHeaderLabel}" Margin="0,0,0,20"/>

                        <!-- Card 1 -->
                        <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="{DynamicResource InfoBrushMuted}" Margin="0,0,10,0">
                                    <TextBlock x:Name="Card1Icon" Text="&#xE8A5;" FontFamily="{StaticResource IconFont}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="Card1Title" Text="--" Style="{StaticResource BodyLabel}" FontSize="12"/>
                                    <TextBlock x:Name="Card1Value" Text="--" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}"/>
                                    <TextBlock x:Name="Card1Trend" Text="--" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Card 2 -->
                        <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="{DynamicResource SuccessBrushMuted}" Margin="0,0,10,0">
                                    <TextBlock x:Name="Card2Icon" Text="&#xE8A5;" FontFamily="{StaticResource IconFont}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="Card2Title" Text="--" Style="{StaticResource BodyLabel}" FontSize="12"/>
                                    <TextBlock x:Name="Card2Value" Text="--" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource SecondaryBrush}"/>
                                    <TextBlock x:Name="Card2Trend" Text="--" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Card 3 -->
                        <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="{DynamicResource SurfaceBrush}" Margin="0,0,10,0">
                                    <TextBlock x:Name="Card3Icon" Text="&#xE7B8;" FontFamily="{StaticResource IconFont}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="Card3Title" Text="--" Style="{StaticResource BodyLabel}" FontSize="12"/>
                                    <TextBlock x:Name="Card3Value" Text="--" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource WarningBrush}"/>
                                    <TextBlock x:Name="Card3Trend" Text="--" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Card 4 -->
                        <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="{DynamicResource SurfaceBrush}" Margin="0,0,10,0">
                                    <TextBlock x:Name="Card4Icon" Text="&#xE9D2;" FontFamily="{StaticResource IconFont}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="Card4Title" Text="--" Style="{StaticResource BodyLabel}" FontSize="12"/>
                                    <TextBlock x:Name="Card4Value" Text="--" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource InfoBrush}"/>
                                    <TextBlock x:Name="Card4Trend" Text="--" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>

