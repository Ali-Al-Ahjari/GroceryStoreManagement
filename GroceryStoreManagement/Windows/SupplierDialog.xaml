<Window x:Class="GroceryStoreManagement.Windows.SupplierDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="إضافة/تعديل مورد" Height="580" Width="450" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" FlowDirection="RightToLeft" Language="ar-YE" ShowInTaskbar="False" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{StaticResource SurfaceBrush}" Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <TextBlock x:Name="DialogTitle" Text="إضافة مورد جديد" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <Button Click="CancelButton_Click" Style="{StaticResource IconButton}" Foreground="White" HorizontalAlignment="Left" Margin="370,0,0,0">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                <StackPanel>

                    <!-- Audit Info -->
                    <Border x:Name="AuditInfoBorder" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15" Visibility="Collapsed">
                        <TextBlock x:Name="AuditInfoText" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                    </Border>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="اسم المورد *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="NameTextBox" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="رقم الهاتف" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="PhoneTextBox" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="البريد الإلكتروني" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="EmailTextBox" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="العنوان" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="AddressTextBox" Style="{StaticResource ModernTextBox}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- Actions -->
            <Border Grid.Row="2" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight">
                    <Button Click="CancelButton_Click" Style="{StaticResource OutlineButton}" Padding="25,12" Margin="10,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="إلغاء" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Click="SaveButton_Click" Style="{StaticResource PrimaryButton}" Padding="25,12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="حفظ" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>

</Window>
