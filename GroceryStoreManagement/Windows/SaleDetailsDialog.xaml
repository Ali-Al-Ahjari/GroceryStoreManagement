<Window x:Class="GroceryStoreManagement.Windows.SaleDetailsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="تفاصيل الفاتورة" Height="650" Width="750" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" FlowDirection="RightToLeft" Language="ar-YE" ShowInTaskbar="False" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{StaticResource SurfaceBrush}" Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- العنوان -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8F1;" FontFamily="{StaticResource IconFont}" FontSize="22" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <StackPanel>
                            <TextBlock Text="تفاصيل الفاتورة" FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="InvoiceNumberText" Text="#0" FontSize="14" Foreground="#FFFFFFAA" Margin="0,0,10,0"/>
                                <TextBlock x:Name="InvoiceDateText" Text="--/--/----" FontSize="12" Foreground="#FFFFFFAA" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Click="MinimizeButton_Click" Style="{StaticResource IconButton}" Foreground="White" Margin="620,0,5,0">
                            <TextBlock Text="&#xE921;" FontFamily="{StaticResource IconFont}" />
                        </Button>
                        <Button Click="CloseButton_Click" Style="{StaticResource IconButton}" Foreground="White" RenderTransformOrigin="1.65,0.593">
                            <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- المحتوى -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- معلومات الفاتورة -->
                <Border Grid.Row="0" Background="{StaticResource BackgroundBrush}" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- العميل -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="العميل" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            <TextBlock x:Name="CustomerNameText" Text="--" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock x:Name="CustomerPhoneText" Text="" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>

                        <!-- عدد العناصر (Code expects this) -->
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="عدد العناصر" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            <TextBlock x:Name="ItemsCountText" Text="0" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- الموظف -->
                        <StackPanel Grid.Column="2" HorizontalAlignment="Left">
                            <TextBlock Text="الموظف" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            <TextBlock x:Name="CreatedByText" Text="--" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- جدول المنتجات -->
                <Border Grid.Row="1" CornerRadius="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <DataGrid x:Name="SaleItemsGrid" Style="{StaticResource ModernDataGridStyle}" BorderThickness="0" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="100" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                            <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="100" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                            <DataGridTextColumn Header="السعر" Binding="{Binding UnitPrice, StringFormat='{}{0:#,##0.##}'}" Width="100" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                            <DataGridTextColumn Header="الخصم %" Binding="{Binding DiscountPercent, StringFormat={}{0:N0}}" Width="100" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                            <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalPrice, StringFormat='{}{0:#,##0.##}'}" Width="100" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                            <DataGridTextColumn Header="مرتجع" Binding="{Binding ReturnedQuantity}" Width="100" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- سجل المرتجعات -->
                <Border Grid.Row="2" Background="{StaticResource BackgroundBrush}" CornerRadius="10" Padding="10" Margin="0,15,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <TextBlock Text="سجل المرتجعات" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="ReturnsCountText" Text="0 عملية" HorizontalAlignment="Left" Foreground="{StaticResource TextMutedBrush}"/>
                        </Grid>

                        <DataGrid Grid.Row="1" x:Name="ReturnsGrid" Style="{StaticResource ModernDataGridStyle}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0" MaxHeight="140">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding ReturnID}" Width="Auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                                <DataGridTextColumn Header="التاريخ" Binding="{Binding ReturnDate, StringFormat=yyyy/MM/dd HH:mm}" Width="Auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                                <DataGridTextColumn Header="القيمة" Binding="{Binding TotalRefund, StringFormat='{}{0:#,##0.##}'}" Width="Auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                                <DataGridTextColumn Header="السبب" Binding="{Binding Reason}" Width="*" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- ملخص الفاتورة -->
                <Border Grid.Row="3" Background="{StaticResource BackgroundBrush}" CornerRadius="10" Padding="15" Margin="0,15,0,0">
                    <Grid>
                        <StackPanel HorizontalAlignment="Left">
                            <TextBlock Text="الإجمالي النهائي" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            <TextBlock x:Name="TotalAmountText" Text="ر.ي. 0" FontWeight="Bold" FontSize="20" Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- أزرار الإجراءات -->
            <Border Grid.Row="2" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Click="ReturnButton_Click" Style="{StaticResource OutlineButton}" Foreground="{StaticResource WarningBrush}" Padding="15,10" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE7A7;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="إرجاع / تعديل" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight">
                        <Button Click="PrintButton_Click" Style="{StaticResource PrimaryButton}" Padding="20,10" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE749;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="طباعة" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Click="CloseButton_Click" Style="{StaticResource OutlineButton}" Padding="20,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="إغلاق" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </Border>

</Window>


