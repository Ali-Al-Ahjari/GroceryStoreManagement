<Window x:Class="GroceryStoreManagement.Windows.BarcodeScannerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="ماسح الباركود" Height="500" Width="600" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" FlowDirection="RightToLeft" Language="ar-YE" ShowInTaskbar="False" Closing="Window_Closing" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{DynamicResource SurfaceBrush}" Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- العنوان -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE114;" FontFamily="{StaticResource IconFont}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="ماسح الباركود" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Click="BtnClose_Click" Style="{StaticResource IconButton}" Foreground="White" HorizontalAlignment="Left" Margin="510,0,0,0">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>
            </Border>

            <!-- عرض الكاميرا -->
            <Grid Grid.Row="1" Margin="20">
                <Border Background="#1A1A2E" CornerRadius="12">
                    <Image x:Name="CameraPreview" Stretch="Uniform"/>
                </Border>

                <!-- مؤشر المسح -->
                <Border Width="250" Height="150" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="3" CornerRadius="8" Opacity="0.8">
                    <Border.Effect>
                        <DropShadowEffect Color="#4379EE" BlurRadius="10" ShadowDepth="0"/>
                    </Border.Effect>
                </Border>

                <TextBlock x:Name="TxtStatus" Text="جاري تشغيل الكاميرا..." Foreground="White" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20"/>
            </Grid>

            <!-- شريط الإجراءات -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="الباركود:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox x:Name="TxtBarcode" Style="{StaticResource ModernTextBox}" Width="188" FlowDirection="LeftToRight" TextAlignment="Center"/>
                        <Button x:Name="BtnManualEntry" Style="{StaticResource OutlineButton}" Padding="15,8" Margin="10,0,0,0" Click="BtnManualEntry_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE70F;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="إدخال يدوي" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="BtnSwitchCamera" Click="BtnSwitchCamera_Click" Style="{StaticResource OutlineButton}" Padding="15,8" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE89A;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="تبديل الكاميرا" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="BtnConfirm" Click="BtnConfirm_Click" Style="{StaticResource PrimaryButton}" Padding="20,8">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE73E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="تأكيد" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </Border>

</Window>
