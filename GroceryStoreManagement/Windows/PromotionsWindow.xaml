<Window x:Class="GroceryStoreManagement.Windows.PromotionsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="العروض والخصومات" Height="600" Width="850" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="CanResize" FlowDirection="RightToLeft" Language="ar-YE" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{DynamicResource SurfaceBrush}" Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8EC;" FontFamily="{StaticResource IconFont}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="العروض والخصومات" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Click="AddPromotion_Click" Style="{StaticResource OutlineButton}" Foreground="White" BorderBrush="White" Padding="15,8" FontSize="12" Margin="650,0,15,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE710;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="إضافة عرض" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Click="Close_Click" Style="{StaticResource IconButton}" Foreground="White">
                            <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Toolbar -->
            <Border Grid.Row="1" Padding="20,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0">
                        <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="FilterComboBox" Width="150" Style="{StaticResource ModernComboBox}" SelectionChanged="FilterComboBox_SelectionChanged">
                            <ComboBoxItem Content="الكل" IsSelected="True"/>
                            <ComboBoxItem Content="العروض السارية"/>
                            <ComboBoxItem Content="العروض المنتهية"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- content -->
            <DataGrid x:Name="PromotionsDataGrid" Grid.Row="2" Style="{StaticResource ModernDataGridStyle}" Margin="20,0,20,20" SelectionChanged="PromotionsDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding PromotionID}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                    <DataGridTextColumn Header="اسم العرض" Binding="{Binding Name}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                    <DataGridTextColumn Header="النوع" Binding="{Binding DisplayType}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                    <DataGridTextColumn Header="القيمة" Binding="{Binding DisplayDiscount}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                    <DataGridTextColumn Header="من" Binding="{Binding StartDate, StringFormat=yyyy/MM/dd}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                    <DataGridTextColumn Header="إلى" Binding="{Binding EndDate, StringFormat=yyyy/MM/dd}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>
                    <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="auto" ElementStyle="{StaticResource DataGridTextColumnElementStyle}"/>

                    <DataGridTemplateColumn Header="الإجراءات" Width="auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Click="EditPromotion_Click" Tag="{Binding PromotionID}" Style="{StaticResource IconButton}" Margin="2">
                                        <TextBlock Text="&#xE70F;" FontFamily="{StaticResource IconFont}" FontSize="14"/>
                                    </Button>
                                    <Button Click="DeletePromotion_Click" Tag="{Binding PromotionID}" Style="{StaticResource IconButton}" Foreground="{DynamicResource DangerBrush}" Margin="2">
                                        <TextBlock Text="&#xE74D;" FontFamily="{StaticResource IconFont}" FontSize="14"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </Border>

</Window>

