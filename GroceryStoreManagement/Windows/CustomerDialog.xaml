<Window x:Class="GroceryStoreManagement.Windows.CustomerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="إضافة/تعديل عميل" Height="800" Width="450" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" FlowDirection="RightToLeft" Language="ar-YE" ShowInTaskbar="False" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{DynamicResource SurfaceBrush}" Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- العنوان -->
            <Border Grid.Row="0" Background="{DynamicResource SecondaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE77B;" FontFamily="{StaticResource IconFont}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock x:Name="DialogTitle" Text="إضافة عميل جديد" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Click="BtnClose_Click" Style="{StaticResource IconButton}" Foreground="White" HorizontalAlignment="Left" Margin="370,0,0,0">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>
            </Border>

            <!-- المحتوى -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                <StackPanel>

                    <!-- اسم العميل -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="اسم العميل *" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtName" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <!-- رقم الهاتف -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="رقم الهاتف" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtPhone" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right"/>
                    </StackPanel>

                    <!-- البريد الإلكتروني -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="البريد الإلكتروني" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtEmail" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right"/>
                    </StackPanel>

                    <!-- العنوان -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="العنوان" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtAddress" Style="{StaticResource ModernTextBox}" Height="80" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top"/>
                    </StackPanel>

                    <!-- الحد الائتماني -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="الحد الائتماني" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtCreditLimit" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right" Text="0"/>
                    </StackPanel>

                    <!-- الحالة -->
                    <StackPanel Margin="0,0,0,15">
                        <CheckBox x:Name="ChkIsActive" Content="عميل نشط" IsChecked="True" Style="{StaticResource ModernCheckBox}"/>
                    </StackPanel>

                    <!-- ملاحظات -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="ملاحظات" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox x:Name="TxtNotes" Style="{StaticResource ModernTextBox}" Height="60" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- أزرار الإجراءات -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="1" x:Name="BtnCancel" Style="{StaticResource OutlineButton}" Padding="25,12" Margin="0,0,10,0" Click="BtnCancel_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="إلغاء" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="2" x:Name="BtnSave" Click="BtnSave_Click" Style="{StaticResource SecondaryButton}" Padding="25,12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="حفظ" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

        </Grid>
    </Border>

</Window>
