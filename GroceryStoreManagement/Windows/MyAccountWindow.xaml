<Window x:Class="GroceryStoreManagement.Windows.MyAccountWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="حسابي" Height="650" Width="520" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" ShowInTaskbar="False" FlowDirection="RightToLeft" Language="ar-YE" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{DynamicResource SurfaceBrush}" Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <TextBlock Text="الحساب الشخصي" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <Button Click="Cancel_Click" Style="{StaticResource IconButton}" Foreground="White" HorizontalAlignment="Left" Margin="420,0,0,0">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                <StackPanel>
                    <TextBlock Text="يمكنك تعديل بيانات الحساب، وتغيير كلمة المرور عند الحاجة." Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,16"/>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="اسم المستخدم" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="UsernameTextBox" Style="{StaticResource ModernTextBox}" IsReadOnly="True" FlowDirection="LeftToRight" TextAlignment="Right"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="الاسم الكامل" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="FullNameTextBox" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="رقم الهاتف" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="PhoneTextBox" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="البريد الإلكتروني" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="EmailTextBox" Style="{StaticResource ModernTextBox}" FlowDirection="LeftToRight" TextAlignment="Right"/>
                        </StackPanel>
                    </Grid>

                    <Rectangle Height="1" Fill="{DynamicResource BorderLightBrush}" Margin="0,8,0,16"/>

                    <TextBlock Text="تغيير كلمة المرور (اختياري)" Style="{StaticResource SubHeaderLabel}" Margin="0,0,0,10"/>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="كلمة المرور الحالية" Style="{StaticResource LabelStyle}"/>
                        <PasswordBox x:Name="CurrentPasswordBox" Style="{StaticResource ModernPasswordBox}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="كلمة المرور الجديدة" Style="{StaticResource LabelStyle}"/>
                        <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource ModernPasswordBox}"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="تأكيد كلمة المرور الجديدة" Style="{StaticResource LabelStyle}"/>
                        <PasswordBox x:Name="ConfirmNewPasswordBox" Style="{StaticResource ModernPasswordBox}"/>
                    </StackPanel>

                    <TextBlock Text="اترك حقول كلمة المرور فارغة إذا لا تريد تغييرها." Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight">
                    <Button Click="Cancel_Click" Style="{StaticResource OutlineButton}" Padding="25,12" Margin="10,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="إلغاء" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Click="Save_Click" Style="{StaticResource PrimaryButton}" Padding="25,12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="حفظ" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
