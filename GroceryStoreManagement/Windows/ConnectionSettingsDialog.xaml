<Window x:Class="GroceryStoreManagement.Windows.ConnectionSettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="إعدادات الاتصال" Height="350" Width="400" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" FlowDirection="RightToLeft" Language="ar-YE" ShowInTaskbar="False" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{StaticResource SurfaceBrush}" Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- العنوان -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE713;" FontFamily="{StaticResource IconFont}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="White"/>
                        <TextBlock Text="إعدادات الاتصال" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Click="BtnClose_Click" Style="{StaticResource IconButton}" Foreground="White" HorizontalAlignment="Left" Margin="320,0,0,0">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>
            </Border>

            <!-- المحتوى -->
            <StackPanel Grid.Row="1" Margin="25,20">

                <TextBlock Text="مسار قاعدة البيانات" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtDatabasePath" Grid.Column="0" Style="{StaticResource ModernTextBox}" IsReadOnly="True"/>
                    <Button x:Name="BtnBrowse" Grid.Column="1" Click="BtnBrowse_Click" Style="{StaticResource IconButton}" Width="42" Height="42" Margin="8,0,0,0">
                        <TextBlock Text="&#xE8B7;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>

                <Border Background="{StaticResource BackgroundBrush}" CornerRadius="10" Padding="15">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="حالة الاتصال:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="TxtConnectionStatus" Text="متصل ✓" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource SecondaryBrush}"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="حجم القاعدة:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="TxtDatabaseSize" Text="-- MB" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Button x:Name="BtnTestConnection" Click="BtnTestConnection_Click" Style="{StaticResource OutlineButton}" Padding="15,10" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="اختبار الاتصال" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

            </StackPanel>

            <!-- أزرار الإجراءات -->
            <Border Grid.Row="2" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight">
                    <Button x:Name="BtnSave" Click="BtnSave_Click" Style="{StaticResource PrimaryButton}" Padding="25,12" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="حفظ" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnCancel" Style="{StaticResource OutlineButton}" Padding="25,12" Click="BtnCancel_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="إلغاء" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

        </Grid>
    </Border>

</Window>
