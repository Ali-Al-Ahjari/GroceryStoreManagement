<Window x:Class="GroceryStoreManagement.Windows.PromotionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="تفاصيل العرض" Height="620" Width="560" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" FlowDirection="RightToLeft" Language="ar-YE" ResizeMode="NoResize" ShowInTaskbar="False" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{StaticResource SurfaceBrush}" Effect="{StaticResource CardShadow}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,15">
                <TextBlock x:Name="DialogTitle" Text="إضافة عرض" Style="{StaticResource HeaderLabel}"/>
                <TextBlock Text="حدد تفاصيل العرض الترويجي" Style="{StaticResource BodyLabel}" Margin="0,4,0,0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="اسم العرض" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="NameTextBox" Style="{StaticResource ModernTextBox}" Margin="0,5,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="نوع الخصم" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="DiscountTypeComboBox" Style="{StaticResource ModernComboBox}" Margin="0,5,0,10">
                                <ComboBoxItem Content="نسبة مئوية" Tag="Percentage" IsSelected="True"/>
                                <ComboBoxItem Content="مبلغ ثابت" Tag="Fixed"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="قيمة الخصم" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="DiscountValueTextBox" Style="{StaticResource ModernTextBox}" Margin="0,5,0,10"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="تاريخ البداية" Style="{StaticResource FormLabel}"/>
                            <DatePicker x:Name="StartDatePicker" Margin="0,5,0,10"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="تاريخ النهاية" Style="{StaticResource FormLabel}"/>
                            <DatePicker x:Name="EndDatePicker" Margin="0,5,0,10"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="الحد الأدنى للشراء" Style="{StaticResource FormLabel}"/>
                    <TextBox x:Name="MinPurchaseTextBox" Style="{StaticResource ModernTextBox}" Margin="0,5,0,10"/>

                    <TextBlock Text="ينطبق على" Style="{StaticResource FormLabel}"/>
                    <ComboBox x:Name="AppliesToComboBox" Style="{StaticResource ModernComboBox}" Margin="0,5,0,10" SelectionChanged="AppliesToComboBox_SelectionChanged">
                        <ComboBoxItem Content="كل المنتجات" Tag="All" IsSelected="True"/>
                        <ComboBoxItem Content="فئة محددة" Tag="Category"/>
                        <ComboBoxItem Content="منتج محدد" Tag="Product"/>
                    </ComboBox>

                    <StackPanel x:Name="CategoryPanel" Visibility="Collapsed">
                        <TextBlock Text="اختر الفئة" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="CategoryComboBox" Style="{StaticResource ModernComboBox}" Margin="0,5,0,10"/>
                    </StackPanel>

                    <StackPanel x:Name="ProductPanel" Visibility="Collapsed">
                        <TextBlock Text="اختر المنتج" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="ProductComboBox" Style="{StaticResource ModernComboBox}" Margin="0,5,0,10" DisplayMemberPath="Name" SelectedValuePath="ProductID"/>
                    </StackPanel>

                    <CheckBox x:Name="IsActiveCheckBox" Content="تفعيل العرض" IsChecked="True" Margin="0,5,0,0"/>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" FlowDirection="LeftToRight" Margin="0,10,0,0">
                <Button Click="Save_Click" Style="{StaticResource PrimaryButton}" Padding="18,8" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="حفظ" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource OutlineButton}" Padding="18,8" Click="Cancel_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="إلغاء" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
