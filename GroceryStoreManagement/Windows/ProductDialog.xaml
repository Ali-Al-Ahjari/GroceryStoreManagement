<Window x:Class="GroceryStoreManagement.Windows.ProductDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="إضافة/تعديل منتج" Height="800" Width="600" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" FlowDirection="RightToLeft" Language="ar-YE" ShowInTaskbar="False" MaxHeight="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Height}" MaxWidth="{Binding Source={x:Static SystemParameters.WorkArea}, Path=Width}">

    <Border CornerRadius="16" Background="{DynamicResource SurfaceBrush}" Effect="{DynamicResource CardShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{DynamicResource PrimaryGradient}" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <TextBlock x:Name="HeaderText" Text="إضافة منتج جديد" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <Button Click="CancelButton_Click" Style="{StaticResource IconButton}" Foreground="White" HorizontalAlignment="Left" Margin="520,0,0,0">
                        <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" />
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                <StackPanel>

                    <!-- Audit Info (Hidden by default) -->
                    <Border x:Name="AuditInfoBorder" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15" Visibility="Collapsed">
                        <TextBlock x:Name="AuditInfoText" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                    </Border>

                    <!-- Code & Barcode -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="كود المنتج" Style="{StaticResource LabelStyle}"/>
                            <Grid>
                                <TextBox x:Name="CodeTextBox" Style="{StaticResource ModernTextBox}" TextChanged="CodeTextBox_TextChanged" Margin="50,0,0,0"/>
                                <Button Click="GenerateCode_Click" Style="{StaticResource IconButton}" HorizontalAlignment="Left" Margin="5,0" ToolTip="توليد كود تلقائي">
                                    <TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}" FontSize="16"/>
                                </Button>
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="الباركود" Style="{StaticResource LabelStyle}"/>
                            <StackPanel Orientation="Horizontal">
                                <Image x:Name="BarcodeImage" Height="30" Margin="0,0,10,0" Visibility="Collapsed"/>
                                <Button x:Name="PrintBarcodeBtn" Click="PrintBarcode_Click" Style="{StaticResource IconButton}" Visibility="Collapsed" ToolTip="طباعة باركود">
                                    <TextBlock Text="&#xE749;" FontFamily="{StaticResource IconFont}" FontSize="16"/>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Grid>

                    <!-- Name -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="اسم المنتج *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="NameTextBox" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <!-- Category & Unit -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="الفئة" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="CategoryComboBox" Style="{StaticResource ModernComboBox}" IsEditable="True"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="الوحدة" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="UnitComboBox" Style="{StaticResource ModernComboBox}" IsEditable="True">
                                <ComboBoxItem Content="قطعة"/>
                                <ComboBoxItem Content="كيلو"/>
                                <ComboBoxItem Content="لتر"/>
                                <ComboBoxItem Content="علبة"/>
                                <ComboBoxItem Content="كرتون"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Prices -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="سعر الشراء *" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="PurchasePriceTextBox" Style="{StaticResource ModernTextBox}" Text="0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="8,0,8,0">
                            <TextBlock Text="سعر البيع *" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="SellingPriceTextBox" Style="{StaticResource ModernTextBox}" Text="0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,15,0,0">
                            <TextBlock Text="الربح:" FontSize="10" Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBlock x:Name="ProfitText" Text="$0.00" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Quantity & Min -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="الكمية الحالية" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="QuantityTextBox" Style="{StaticResource ModernTextBox}" Text="0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="الحد الأدنى" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="MinQuantityTextBox" Style="{StaticResource ModernTextBox}" Text="1"/>
                        </StackPanel>
                    </Grid>

                    <!-- Supplier -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="المورد" Style="{StaticResource LabelStyle}"/>
                        <ComboBox x:Name="SupplierComboBox" Style="{StaticResource ModernComboBox}"/>
                    </StackPanel>

                    <!-- Expiry Date -->
                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="تاريخ الانتهاء (اختياري)" Style="{StaticResource LabelStyle}"/>
                        <DatePicker x:Name="ExpiryDatePicker" FlowDirection="LeftToRight"/>
                    </StackPanel>


                </StackPanel>
            </ScrollViewer>

            <!-- Footer Actions -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Click="SaveAndAddNewButton_Click" Style="{StaticResource OutlineButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="حفظ وإضافة جديد" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" FlowDirection="LeftToRight">
                        <Button Click="CancelButton_Click" Style="{StaticResource OutlineButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="إلغاء" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Click="SaveButton_Click" Style="{StaticResource PrimaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE74E;" FontFamily="{StaticResource IconFont}" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="حفظ" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>

</Window>

